1. Create a git repo and clone it in local
2. Create code.py file and write a code in it( it will get saved in a 'data' folder)
3. Do a git add-commit-push before initializing DVC
# pip install dvc
4. Now we do dvc init (creates .dvcignore,.dvc)
5. Now make a folder named S3
6. Now we do dvc remote add -d myremote S3
5. Next add dvc add data/ to track contents of the data folder